{% load licensemapper %}
{
    "metadata": {
        "json-ld": {
            "ebucore:hasMimeType": [
                "{{media.mime_type}}"
            ],
            "ebucore:filename": ["{{media.file}}"],
            "edm:rights": [
                "{{media.license.source | phaidra_eq_license}}"
            ]

        }
    }
}
